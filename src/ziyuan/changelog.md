# 更新日志

## v1.2.8 (2026-06-04)

### 问题修复
- 修复更新到新版本后，开启开机自启可能提示“启动快捷方式内容与当前程序不匹配”的问题。
- 开机自启现在会识别并重建旧安装路径、坏掉或不匹配的启动快捷方式。
- 放宽 Windows 启动快捷方式工作目录校验，兼容系统读回空工作目录、路径大小写、短路径和解析后路径差异。
- 启动快捷方式图标路径差异不再作为开机自启失败条件。

## v1.2.7 (2026-06-04)

### 功能支持
- 设置页新增原文和译文字号调节，支持滑动调整并提供实时字号示例。

### 优化改进
- 优化主窗口、设置页和关于页的字号表现，字号设置重启后会继续生效。
- 美化关于页并适配浅色、粉色和深色主题，同时禁用关于页右键菜单。
- 优化 mini 小窗模式：移除窗口标题，修复圆角边框畸形，放大译文显示字号，降低文字颗粒感。
- 顶部状态区文案由“当前翻译服务提供”精简为“翻译服务”，减少主窗口顶部栏拥挤。

### 问题修复
- 修复 1.2.7 更新说明被错误写成问号乱码的问题。
- 修复新安装旧版本后，更新日志窗口与“发现新版本”窗口可能同时弹出并互相阻塞的问题；现在会先显示更新日志，关闭后再显示更新提示。
- 修复设置页标签文字可能超出标签框的问题。
- 修复 mini 小窗切回主窗口时可能没有正确同步窗口模式状态的问题。
- 修复 mini 小窗旧输出路径仍调用 QTextEdit 接口，导致 QLabel 输出控件潜在报错的问题。

## v1.2.6 (2026-06-02)

### 问题修复
- 修复完整安装器在已有旧版本时仍默认安装到 C 盘的问题，安装器会优先复用注册表、运行进程、快捷方式或常见旧安装目录中的已有安装位置。
- 修复 1.2.5 兜底安装器可能和旧版本并存的问题；升级到 1.2.6 时会把错误生成的重复安装目录移入用户备份目录，并重建桌面/开始菜单快捷方式到正确安装目录。
- Inno 安装脚本恢复复用历史安装目录，避免后续安装包再次绕开用户原本选择的路径。

## v1.2.5 (2026-05-31)

### 问题修复
- 修复 Windows 全量静默更新可能替换失败后仍重启旧版本的问题，更新脚本会强制处理旧进程、保留持久日志并校验目标版本。
- 补强 1.2.5 安装器：安装前会清理安装目录内残留的内置引擎进程，用于修复 1.2.4 旧更新器被 `deeplx.exe` 占用阻断后的安装。
- 在线更新包改为携带 `deeplx.exe.payload`，避免 1.2.4 旧更新器覆盖正在运行的内置引擎导致回滚；缺少引擎时会自动写入用户缓存。
- 更新包新增版本清单，安装前后都会校验包内版本，防止文件名是新版本但内容仍是旧版本。
- 修复 Windows 打包版开机自启无法开启的问题：自启入口不再依赖 pywin32，改用系统 PowerShell/WScript 创建启动快捷方式。
- 修复 Nuitka 打包版自启命令识别错误的问题，避免生成带 `-m src.main` 的无效启动命令。
- 开机自启状态校验会读取快捷方式目标、参数和工作目录，旧的错误入口会在启动时自动修复。
- 修复发行版 Qt 插件路径探测错误导致的潜在启动失败。
- 修复翻译服务切换失败后旧服务仍可能继续翻译的问题。
- 修复 Achord 内置引擎本地请求可能经过系统代理的问题。
- 修复托盘不可用时关闭窗口后程序仍在后台残留的问题。
- 修复重复启动程序时只能弹提示、无法唤醒已运行窗口的问题。
- 修复多个配置对象连续保存时可能互相覆盖的问题。
- 修复快捷键变更失败后旧快捷键可能丢失的问题。
- 修复旧版 AI 通用接口配置已存在 profiles 时不会迁移当前厂商配置的问题。
- 修复设置页切换 AI 厂商后点击取消仍可能保存草稿配置的问题。
- 修复源语言选择无法持久化的问题，源语言和目标语言都会按用户选择保存。
- 修复语言搜索框、更新说明和通用对话框文本区域右键菜单仍出现英文的问题。
- 关于页完整展示 DeepLX / OwO Network MIT License 和版权信息，避免固定宽度下被截断。
- 更新失败后会在下次启动明确提示失败原因与日志路径，避免用户误以为已经升级成功。

## v1.2.4 (2026-05-31)

### 功能支持
- 新增 DeepL 翻译服务，支持输入 DeepL API Key 后识别 Free / Pro 身份
- 新增 Achord 内置引擎，基于 DeepLX 静默运行，无需登录，并支持单独检测与更新翻译引擎
- 保留 Google 翻译为默认服务
- AI 通用接口继续支持智谱、OpenAI、DeepSeek、通义千问、字节豆包、Google Gemini 和自定义配置

### 优化改进
- 引入 QtAwesome 图标，替换主界面标题栏、工具按钮和信息按钮中的旧 SVG 图标
- 优化系统字体策略，禁用并移除 PingFang 字体，改用系统黑体
- 移除内置字体文件和打包引用，发行包仅使用系统字体
- 统一主题样式和迷你窗口字体声明为 SimHei，避免控件间字体回退不一致
- 设置页 DeepL 配置仅保留 API Key 输入与身份验证入口
- 设置页输入框右键菜单改为中文
- Google 服务重试时先显示“正在连接”，只有真实连通后才显示“已连接”
- Windows 在线更新改为全量静默替换，不再启动安装器

### 问题修复
- 修复开机自启在开发环境和安装环境下目标路径、工作目录、图标路径不一致的问题
- 修复开机自启设置保存失败时仍写入配置的问题
- 修复更新下载缓存残留、下载路径和安装提示的若干边界问题
- 修复静默更新失败回滚、卸载文件保留、跨盘安装和异常版本号解析问题
- 修复配置文件异常损坏后无法自动恢复的问题，配置保存改为原子写入
- 修复异常退出后单实例锁残留导致无法再次启动的问题
- 修复 Google 翻译连接未校验证书的问题
- 修复窗口事件过滤器被重复定义导致的缩放和激活事件处理不稳定问题
- 修复 Mini 模式托盘入口、快捷键启停和翻译错误状态显示的若干一致性问题
- 修复主题菜单、托盘菜单、更新弹窗等在不同主题下的样式不一致问题

### 其他变更
- 移除旧版 Achord 自研模型配置，内置引擎改为本机静默托管
- 关于页补充 DeepLX / OwO Network MIT License 第三方组件说明
- 版本号更新为 1.2.4

## v1.2.0 (2025-01-21)

### 功能支持 ✨
- 增加更新流程说明文档（PACKAGING_AND_UPDATE.md）

### 优化改进 🚀
- 设置窗口超限内容支持滚动
- 设置窗口主题适配完善（深色/浅色/粉色）
- 更新日志窗口跟随主题样式

### 问题修复 🔧
- 修复长文本换行标记被翻译导致的异常字符
- 修复 aiohttp 会话未关闭的警告

## v1.1.0 (2024-12-24)

### 功能支持 ✨
- 支持多种翻译服务：Google、DeepSeek、豆包
- 全局快捷键：Ctrl+C,C 快速呼出翻译
- 多主题支持：深色、浅色、粉色
- 系统托盘支持
- 自动更新功能

### 优化改进 🚀
- 优化界面设计，采用苹果设计风格
- 改进翻译响应速度
- 增加配置保存功能
- 添加窗口拖拽和缩放功能

### 问题修复 🔧
- 修复了部分快捷键冲突问题
- 修复了托盘图标显示异常
- 修复了主题切换时的闪烁问题
- 修复了快捷键无法使用的问题

### 其他变更 📝
- 添加了详细的使用说明
- 优化了安装程序
- 改进了错误提示信息
