# 主界面重构视觉 QA

## 对比对象

- 参考图：用户提供的浅色双栏翻译界面。
- 实现截图：969 × 684，浅色主题，包含语言工具栏、服务状态、原文和译文示例。

## 检查结果

1. 信息层级：通过。标题栏、翻译工具栏、原文面板、译文面板的层级与参考图一致。
2. 布局比例：通过。原文和译文面板使用相同伸缩比例，实测宽度为 465 px 和 464 px。
3. 操作入口：通过。主题、迷你模式、设置集中在顶部工具栏；标题栏不再保留重复入口。
4. 面板操作：通过。原文清空和译文复制位于面板标题栏，旧浮动复制按钮在主界面模式下隐藏。
5. 视觉样式：通过。浅色主题采用浅灰背景、白色圆角工具栏和面板、低对比边框；深色和粉色主题使用各自配色。
6. 状态与溢出：通过。空文本时操作按钮禁用，存在文本时启用；AI 状态栏保留在译文面板底部。
7. 图标规范：通过。新增入口使用 QtAwesome 图标，没有使用 emoji 作为界面图标。

## 验证说明

- 离屏 Qt 环境未暴露系统中文字体，截图中的中文字符无法正常渲染；控件文本、对象结构和 Windows 实际运行路径均保留中文。
- 参考图与实现图的结构、间距、圆角、双栏比例和操作位置已人工比对。

final result: passed
